# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.1"
metadata:
  name: zzrcaf
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: test
          url: https://fiorides.transelec.cl/
          client: '400'
        app:
          name: ZZRRCAF
          description: Resumen CAF
          package: Z001
          transport: DS4K906962
        exclude:
          - /test/
